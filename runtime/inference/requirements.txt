# VoxPassport — Python Runtime Dependencies
# Install with: pip install -r runtime/inference/requirements.txt

# Core ML
torch>=2.2.0
torchaudio>=2.2.0

# Hugging Face / local model execution
# NVIDIA's current Parakeet TDT v3 model card still requires Transformers from
# source until its TDT classes land in a stable Transformers release.
transformers @ git+https://github.com/huggingface/transformers.git
accelerate>=0.27.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# NVIDIA NeMo is intentionally optional because the default Parakeet -> MiLMMT
# path does not need it. The optional Streaming Sortformer diarization sidecar
# requires NeMo ASR after its weights are downloaded from the Model Hub:
#   pip install Cython packaging
#   pip install "nemo_toolkit[asr]"

# Audio I/O and DSP
soundfile>=0.12.0
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# Hardware/system telemetry
psutil>=5.9.0

# IPC / HTTP / WebSockets
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0
asyncio-throttle>=1.0.2
websockets>=12.0
aiohttp>=3.9.0
requests>=2.31.0

# Audio tools used by utilities/benchmarks
SpeechRecognition>=3.10.0
pydub>=0.25.1

# Configuration
pyyaml>=6.0

# Tests / development
pytest>=8.0.0
pytest-asyncio>=0.23.0
mypy>=1.8.0

# Voice-profile persistence
cryptography>=42.0.0
