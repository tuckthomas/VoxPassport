# Fallback pip dependency set for the coqui-xtts runtime profile.
# `scripts/manage_runtime_profile.py` prefers the independent uv project under
# runtime/profiles/coqui-xtts; this file is used when uv is not available.
# Torch/TorchAudio/TorchCodec are installed separately from the official cu130 index.
coqui-tts==0.27.5
transformers>=4.45,<5
aiohttp>=3.9
huggingface-hub>=0.20
numpy>=1.24
soundfile>=0.12
